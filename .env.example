DATABASE_URL=
BETTER_AUTH_SECRET=
NEXT_PUBLIC_VERCEL_APP_CLIENT_ID=
VERCEL_APP_CLIENT_SECRET=
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Optional: override the app origin for a custom production domain.
BETTER_AUTH_URL=

# Optional: enable hosted Vercel Connect integrations.
SLACK_CONNECTOR=
LINEAR_CONNECTOR=
NOTION_CONNECTOR=
SENTRY_CONNECTOR=

# Optional: self-hosted MCP servers for X, LinkedIn, and Threads. These platforms
# have no official hosted MCP server, so point each *_MCP_URL at a server you run.
# Set the matching *_MCP_TOKEN only if your server expects a bearer token.
X_MCP_URL=
X_MCP_TOKEN=
LINKEDIN_MCP_URL=
LINKEDIN_MCP_TOKEN=
THREADS_MCP_URL=
THREADS_MCP_TOKEN=
